# FireFlow AI AI Coding Guide

## 1. 목적

이 문서는 AI 코딩 도구가 FireFlow AI 프로젝트를 수정하거나 기능을 추가할 때 따라야 하는 규칙을 정의합니다.

작업을 시작하기 전에 반드시 다음 문서를 순서대로 확인합니다.

1. `README.md`
2. `docs/IDEA.md`
3. `docs/ARCHITECTURE.md`
4. 현재 작업 대상 코드

---

## 2. 프로젝트 핵심 전제

- FireFlow AI는 시민, 소방관, 중앙 통제 역할을 분리한 소방안전 웹서비스 MVP입니다.
- 프론트엔드는 React + Vite, 백엔드는 FastAPI를 사용합니다.
- MVP에서는 실제 인증과 외부 API를 사용하지 않습니다.
- AI 응답, 지도, 공공데이터, 건물 정보는 더미 구현을 사용합니다.
- 현재 목표는 실서비스 완성이 아니라 역할별 핵심 흐름의 안정적인 시연입니다.

---

## 3. 작업 전 필수 절차

코드를 수정하기 전에 다음을 수행합니다.

1. 관련 폴더와 기존 파일을 먼저 확인합니다.
2. 이미 존재하는 컴포넌트, 서비스, 스키마를 재사용할 수 있는지 확인합니다.
3. 변경할 파일과 영향을 받는 기능을 파악합니다.
4. 아키텍처를 변경해야 한다면 변경 이유를 먼저 설명합니다.
5. 요청과 직접 관계없는 대규모 리팩터링은 하지 않습니다.

---

## 4. 구조 유지 규칙

### 프론트엔드

- 페이지는 `pages/`에 배치합니다.
- 재사용 UI는 `components/`에 배치합니다.
- API 호출은 `services/`에서만 수행합니다.
- 페이지와 컴포넌트 내부에 API URL을 직접 작성하지 않습니다.
- 위험도 계산, 출동 우선순위 계산 등 핵심 규칙을 프론트엔드에 중복 구현하지 않습니다.
- 하나의 컴포넌트가 지나치게 많은 역할을 담당하지 않게 분리합니다.

### 백엔드

- 라우터는 요청 검증과 서비스 호출만 담당합니다.
- 비즈니스 로직은 `services/` 또는 `domain/`에 작성합니다.
- JSON 파일 접근은 `repositories/`에 작성합니다.
- AI·지도·공공데이터 관련 코드는 `providers/`에 작성합니다.
- Pydantic Schema 없이 임의 Dictionary 응답을 만들지 않습니다.
- 공통 응답 형식과 오류 형식을 유지합니다.

---

## 5. 역할 분리 규칙

### 시민

시민 화면은 즉시 실행 가능한 대피 행동을 우선합니다.

- 불필요한 소방 전술 용어를 노출하지 않습니다.
- 확정되지 않은 대피 경로나 건물 상태를 사실처럼 표현하지 않습니다.
- 119 및 현장 안내 우선 문구를 포함합니다.

### 소방관

소방관 화면은 신고와 건물 정보를 압축한 브리핑을 제공합니다.

- AI가 화재 규모, 구조 안정성, 인명 상태를 확정하지 않습니다.
- 추정 정보와 확인된 정보를 구분합니다.
- 현장에서 확인해야 할 항목을 별도로 표시합니다.

### 중앙 통제

중앙 통제 화면은 신고 비교와 우선순위 확인에 집중합니다.

- 위험도, 출동 상태, 신고 시각을 기준으로 정렬합니다.
- 역할별 상세 정보를 한 화면에 무분별하게 섞지 않습니다.

---

## 6. API 규칙

- 모든 API 경로는 `/api/v1` 아래에 작성합니다.
- 자원 이름은 복수형 명사를 사용합니다.
- HTTP Method 의미를 지킵니다.
  - 조회: `GET`
  - 생성: `POST`
  - 일부 변경: `PATCH`
  - 삭제: `DELETE`
- 성공 응답과 실패 응답의 공통 구조를 유지합니다.
- 존재하지 않는 자원은 `404`를 반환합니다.
- 잘못된 요청값은 `422`를 반환합니다.
- API 응답 필드 이름을 프론트엔드 편의만을 위해 임의 변경하지 않습니다.

---

## 7. 더미 데이터 규칙

- 외부 API가 필요한 기능은 먼저 Dummy Provider로 구현합니다.
- 더미 데이터는 실제 API 응답 구조와 최대한 유사한 내부 모델로 변환합니다.
- UI 컴포넌트 안에 더미 데이터를 직접 작성하지 않습니다.
- 신고와 건물의 ID 관계를 유지합니다.
- 실제 개인을 식별할 수 있는 이름, 전화번호, 정확한 주소를 사용하지 않습니다.
- 정상 시나리오뿐 아니라 빈 데이터와 일부 정보 누락 시나리오도 포함합니다.

---

## 8. 코드 품질 규칙

- 함수와 변수 이름은 역할을 명확히 드러내게 작성합니다.
- 한 함수는 하나의 주요 책임을 갖습니다.
- 중복 코드를 발견하면 공통 함수 또는 컴포넌트로 분리합니다.
- 불필요한 추상화는 만들지 않습니다.
- 주석은 코드가 무엇을 하는지보다 왜 그렇게 했는지 설명할 때 사용합니다.
- 임시 `console.log`, `print`, 사용하지 않는 import를 남기지 않습니다.
- 환경변수와 비밀키를 코드에 직접 작성하지 않습니다.

---

## 9. UI 규칙

- 시민, 소방관, 중앙 통제 화면은 서로 구분되는 제목과 정보 구조를 가집니다.
- 공통 레이아웃과 Badge, Card, Error State는 재사용합니다.
- 위험도는 텍스트도 함께 표시하여 색상만으로 구분하지 않습니다.
- 로딩, 빈 데이터, 오류 상태를 반드시 처리합니다.
- 지도 미연동 상태는 실제 지도처럼 속이지 않고 Placeholder로 표시합니다.
- 버튼과 상태 라벨은 한국어로 통일합니다.

---

## 10. 안전 표현 규칙

다음 표현은 사용하지 않습니다.

- “AI가 안전한 대피 경로를 보장합니다.”
- “실제 소방 출동이 완료되었습니다.”
- “건물 붕괴 위험이 없습니다.”
- “인명 피해가 없습니다.”

대신 다음과 같이 표현합니다.

- “현재 입력된 정보를 기준으로 한 참고 안내입니다.”
- “실제 상황에서는 119와 현장 안내를 우선하세요.”
- “현장 확인이 필요한 정보입니다.”
- “시연용 출동 상태입니다.”

---

## 11. 테스트 규칙

기능을 추가하거나 변경하면 다음을 확인합니다.

- 정상 응답
- 데이터 없음
- 존재하지 않는 ID
- 일부 필드 누락
- API 오류
- 역할별 데이터가 섞이지 않는지

백엔드 기능은 가능하면 pytest 테스트를 함께 작성합니다.
프론트엔드 UI 변경은 최소한 수동 시연 절차를 기록합니다.

---

## 12. 금지 사항

- 사용자 요청 없이 기술 스택 변경
- 사용자 요청 없이 폴더 구조 전면 개편
- 실제 API 키 또는 개인정보 커밋
- 프론트엔드에서 JSON 파일 직접 읽기
- 라우터에 복잡한 비즈니스 로직 작성
- 동일한 데이터 모델을 역할별로 중복 정의
- 실제 연동되지 않은 기능을 연동된 것처럼 표현
- 테스트 없이 핵심 API 응답 구조 변경
- 기존 기능을 제거하고 설명하지 않기

---

## 13. 작업 완료 보고 형식

작업을 마친 뒤 다음 형식으로 정리합니다.

```text
## 구현 내용
- 구현한 기능

## 변경 파일
- 경로: 변경 이유

## API 변경
- 추가 또는 변경된 Endpoint
- 요청·응답 구조

## 확인 방법
1. 실행 명령어
2. 접속 경로
3. 테스트 순서

## 남은 제한사항
- 더미 구현 또는 미연동 기능
```

---

## 14. Definition of Done

기능은 다음 조건을 만족해야 완료로 봅니다.

- 요청한 기능이 실제 화면 또는 API에서 동작함
- 기존 역할별 화면이 깨지지 않음
- 공통 API 응답 형식을 유지함
- 로딩, 빈 데이터, 오류 상태를 처리함
- 하드코딩된 비밀키가 없음
- 새 구조가 `ARCHITECTURE.md`와 충돌하지 않음
- 실행 방법과 변경 파일이 정리됨
- 실제 미연동 기능이 명확히 표시됨
